<Window x:Class="WpfApp1.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="44*"/>
            <RowDefinition Height="35*"/>
            <RowDefinition Height="Auto" MinHeight="21.96"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Добро пожаловать" 
                   FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20" Width="188"/>

        <TextBlock Text="Имя пользователя:" Margin="0,47,0,5" Grid.RowSpan="2"/>
        <TextBox Grid.Row="2" x:Name="UsernameTextBox" Margin="0,5,0,20"/>

        <TextBlock Grid.Row="2" Text="Пароль:" Margin="0,35,0,4"/>
        <PasswordBox Grid.Row="4" x:Name="PasswordBox" Margin="0,0,0,1" RenderTransformOrigin="0.5,0.5">
            <PasswordBox.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="0.036"/>
                    <TranslateTransform/>
                </TransformGroup>
            </PasswordBox.RenderTransform>
        </PasswordBox>

        <StackPanel Grid.Row="6" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,2,0,0" Width="210">
            <Button Content="Войти" Click="LoginBtn_Click" 
                    Width="100" Margin="0,0,10,0"/>
            <Button Content="Регистрация" Click="RegisterBtn_Click" 
                    Width="100"/>
        </StackPanel>
    </Grid>
</Window>